# Client feature flag — enables fetch to /api/sathi (never put the API key here)
VITE_ENABLE_GEMINI=false

# Server-only — loaded by Vite middleware / Node. NEVER prefix with VITE_
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
